# Shell scripts, systemd units and Caddy config are consumed on Linux.
# CRLF breaks them: bash reports "bad interpreter: /usr/bin/env bash^M".
* text=auto eol=lf
*.sh   text eol=lf
*.mjs  text eol=lf
Caddyfile      text eol=lf
*.service      text eol=lf
